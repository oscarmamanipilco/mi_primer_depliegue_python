services:
  backend:
    build: .
    volumes:
      - ./app:/code
    environment:
      - DATABASE_URL=sqlite:///./test.db

  gateway:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
